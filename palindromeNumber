class Solution {
public:
    bool isPalindrome(int x) 
    {
      stringstream ss;
      ss << x;
      string str = ss.str();
      
      string ans = "";
      for(int i = str.size() - 1; i >= 0; --i)
      {
         ans += str[i];
      }
      return (str == ans);
    }
   
};
